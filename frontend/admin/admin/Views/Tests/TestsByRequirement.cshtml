@model IEnumerable<admin.Api.Model.Response.TestEntireResponse>

@{
    //ViewBag.Title = "Carreira - " + Model.CareerMapResponse.CareerMapName;
    //var f = Model.FirstOrDefault();
    //ViewData["Title"] = Model.FirstOrDefault().Description;
}
<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item "><a href="@Url.Action("Index", "CareerMaps")">Mapas de carreira</a></li>
        @*<li class="breadcrumb-item"><a href="@Url.Action("CompanyPositionsCareerMapDetail", "CareerMaps", new { careerMapId = f.Model.CareerMap.CareerMapId })">@Model.CareerMap.CareerMapName</a></li>*@
        @*<li class="breadcrumb-item active">@Model.FirstOrDefault().Description</li>*@
    </ol>
</nav>

<div class="d-grid gap-2 d-md-flex justify-content-md-end">
    <button class="btn btn-primary me-md-2" type="button">Adicionar Mapa</button>
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TestId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TestId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /*id=item.TestId*/ }) |
                @Html.ActionLink("Details", "Details", new { testId=item.TestId }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
